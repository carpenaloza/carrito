<nav class="navbar navbar_cliente navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid fondo_navbar">
    <a class="navbar-brand">
      <%= link_to admin_pedidos_path, class: "nav-link" do %>
      <i class="bi bi-person-circle"></i> ADMIN </a>
          <% end %>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNav">
    <%# <div class="nav-item dropdown"> %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to root_path, class: "nav-link" do %>
          <i class="bi bi-house"></i> Inicio </a>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to root_path, class: "nav-link" do %>
          <i class="bi bi-bookmarks"></i> Catálogo </a>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to carrito_path, class: "nav-link" do %>
          <i class="bi bi-cart"></i> Carrito <span class="badge bg-primary"> <%= @carro.carros_contenidos.map{ |p| p.cantidad }.sum %> </span> </a>
          <% end %>
        </li>
        
      </ul>
    </div>
    

        <div class="nav-item dropdown">

          <% if usuario_ha_iniciado_sesion? %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdawn" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= @usuario_actual.nombre %>
            </a>
            <div class="dropdown-menu">
              <%= link_to 'Cerrar sesión', cerrar_sesion_path, class: 'btn btn-warning', method: :delete, data: { confirm: 'está seguro de cerrar sesión?'} %>
            </div>

          <% else %>
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdawn" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          Iniciar Sesión
          </a>
          <%= form_with(url: iniciar_sesion_path, class: 'dropdown-menu p-4') do |formulario| %>
            <div class="mb-3">
              <%= formulario.label :correo, class: 'form-label' %>
              <%= formulario.text_field :correo, class: 'form-control', placeholder: 'nombre@ejemplo.com' %>
            </div>
            <div class="mb-3">
              <%= formulario.label :password, class: 'form-label' %>
              <%= formulario.text_field :password, class: 'form-control'%>
            </div>
            <div class="mb-3">
              <%= formulario.submit 'Iniciar Sesión', class: 'btn' %>
            </div>
          <% end %>
          <% end %>
          
        </div>
        
</nav>